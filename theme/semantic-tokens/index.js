import { semanticColors } from "../foundations/colors";
import { semanticFontSizes } from "../foundations/font-sizes";
import { semanticSizes } from "../foundations/sizes";

export const semanticTokens = {
  colors: semanticColors,
  fontSizes: semanticFontSizes,
  sizes: semanticSizes,
};
